other-bar: spam
other-foo: from foo development
democonfigclient.message: hello from dev profile
permissions:
  entity:
    permissions:
      - type: "other"
        id: "RPM001other"
        description: "other edit registered planning titles"
        actions:
          - "update"
